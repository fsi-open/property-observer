language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1

env:
  - COMPOSER_FLAGS=""
  - COMPOSER_FLAGS="--prefer-lowest"

before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar update --dev $COMPOSER_FLAGS

script: bin/phpunit
